@model List<string>

@if (Model.Count == 0)
{
  <div class="alert alert-danger">Детейл с таким названием не встречается в шаблонах оформления заявок.</div>
}
else
{
  <ul>
    @foreach (string fileName in Model)
    {
      <li>@fileName</li>
    }
  </ul>
}